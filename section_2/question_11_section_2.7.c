#include <stdbool.h>
#include <stdio.h>

int main(void) {
  for (int i = 0; i < 10; i++) {
  }
  // If(true) not a valid keyword
  // main() not a valid keyword
  // printf("hello"); part of stdio.h but not a keyword
  while (false) {
  }
}
